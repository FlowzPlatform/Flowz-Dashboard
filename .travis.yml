sudo: required

language: node

services:
  - docker

branches:
  only:
  - master
  - demo

jobs:
  include:
    - stage: Flowz-Dashboard-Fronted-Flowz
      script:
      - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
      - docker build -t flowz_dashboard_fronted_flowz:$TRAVIS_BRANCH .
      - docker images
      - docker tag flowz_dashboard_fronted_flowz:$TRAVIS_BRANCH $DOCKER_USERNAME/flowz_dashboard_fronted_flowz:$TRAVIS_BRANCH
      - docker push $DOCKER_USERNAME/flowz_dashboard_fronted_flowz:$TRAVIS_BRANCH
    - stage: Upgrade Rancher Service
      script:
      - 'if [ ${TRAVIS_BRANCH} = "demo" ]; then
            bash ./upgrade_demo.sh;
         else
            bash ./upgrade.sh;
         fi'
    - stage: Finish Rancher Service
      script:
      - 'if [ ${TRAVIS_BRANCH} = "demo" ]; then
            bash ./finish_demo.sh;
         else
            bash ./finish.sh;
         fi'

notifications:
  email:
    recipients:
      - arpitap@officebeacon.com
      - kaushalm@officebrain.com
      - anuj@officebrain.com
      - naveeng@officebrain.com
      - uhirani@officebrain.com
      - igandhi@officebrain.com
    on_success: always
    on_failure: always         
